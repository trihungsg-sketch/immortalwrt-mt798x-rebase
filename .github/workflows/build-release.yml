name: Build Release
yarn install --ignore-scripts
on:
  push:
    tags:
      - "v[0-9]+.[0-9]+.[0-9]+"
  workflow_dispatch:
    inputs:
      tag:
        description: "Existing release tag (e.g. v1.0.0)"
        required: true
        type: string

concurrency:
  group: build-release-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  prepare:
    runs-on: ubuntu-latest
    outputs:
      tag: ${{ steps.meta.outputs.tag }}
      version: ${{ steps.meta.outputs.version }}
    steps:
      - name: Resolve tag and version
        id: meta
        run: |
          set -euo pipefail
          if [[ "${{ github.event_name }}" == "workflow_dispatch" ]]; then
            TAG="${{ github.event.inputs.tag }}"
          else
            TAG="${GITHUB_REF_NAME}"
          fi
          if [[ ! "${TAG}" =~ ^v[0-9]+\.[0-9]+\.[0-9]+$ ]]; then
            echo "::error::Invalid tag (need vMAJOR.MINOR.PATCH): ${TAG}" >&2
            exit 1
          fi
          VERSION="${TAG#v}"
          echo "tag=${TAG}" >> "${GITHUB_OUTPUT}"
          echo "version=${VERSION}" >> "${GITHUB_OUTPUT}"


  build-firmware:
    needs: prepare
    runs-on: ubuntu-latest
    timeout-minutes: 360
    steps:
      - uses: actions/checkout@v7

      - name: Maximize build disk space
        run: |
          sudo rm -rf /usr/share/dotnet
          sudo rm -rf /usr/local/lib/android
          sudo rm -rf /opt/ghc
          sudo rm -rf /opt/hostedtoolcache/CodeQL
          sudo docker image prune --all --force 2>/dev/null || true
          df -h /

      - name: Install build dependencies
        run: bash scripts/install-deps.sh

      - name: Restore toolchain cache
        id: cache-toolchain
        uses: actions/cache/restore@v5
        with:
          path: |
            staging_dir/host
            staging_dir/toolchain-aarch64_cortex-a53_gcc-*
          key: openwrt-toolchain-filogic-aarch64-ubuntu2404-${{ hashFiles('feeds.conf.default') }}-v1
          restore-keys: |
            openwrt-toolchain-filogic-aarch64-ubuntu2404-

      - name: Restore download cache
        id: cache-dl
        uses: actions/cache/restore@v5
        with:
          path: dl
          key: openwrt-dl-filogic-ubuntu2404-${{ hashFiles('feeds.conf.default') }}-v1-${{ github.run_id }}
          restore-keys: |
            openwrt-dl-filogic-ubuntu2404-${{ hashFiles('feeds.conf.default') }}-v1-

      - name: Build firmware and collect artifacts
        run: bash scripts/build-viettel.sh defconfig/viettel-only.config

      - name: Verify artifacts exist
        run: test "$(ls -1 dist/ | wc -l)" -ge 1

      - name: Upload firmware artifacts
        uses: actions/upload-artifact@v7
        with:
          name: immortalwrt-filogic-${{ needs.prepare.outputs.version }}
          path: dist/
          if-no-files-found: error
          retention-days: 14

      - name: Free disk before cache save
        if: always()
        run: |
          echo "=== Before cleanup ==="
          df -h /
          du -sh build_dir bin tmp staging_dir dl 2>/dev/null || true
          rm -rf build_dir bin tmp logs feeds
          rm -rf staging_dir/target-* staging_dir/packages
          echo "=== After cleanup (keep host + toolchain + dl for cache) ==="
          df -h /
          du -sh staging_dir/host staging_dir/toolchain-* dl 2>/dev/null || true

      - name: Save toolchain cache
        if: always() && steps.cache-toolchain.outputs.cache-hit != 'true'
        uses: actions/cache/save@v5
        with:
          path: |
            staging_dir/host
            staging_dir/toolchain-aarch64_cortex-a53_gcc-*
          key: openwrt-toolchain-filogic-aarch64-ubuntu2404-${{ hashFiles('feeds.conf.default') }}-v1

      - name: Save download cache
        if: always()
        uses: actions/cache/save@v5
        with:
          path: dl
          key: openwrt-dl-filogic-ubuntu2404-${{ hashFiles('feeds.conf.default') }}-v1-${{ github.run_id }}


  release:
    needs: [prepare, build-firmware]
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Download build artifacts
        uses: actions/download-artifact@v8
        with:
          path: release-assets
          merge-multiple: true

      - name: List release files
        run: find release-assets -type f -ls

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v3
        with:
          tag_name: ${{ needs.prepare.outputs.tag }}
          name: ImmortalWrt ${{ needs.prepare.outputs.version }} — NR3053 & 32X6
          generate_release_notes: true
          make_latest: true
          overwrite_files: true
          body: |
            ## ImmortalWrt ${{ needs.prepare.outputs.version }}

            ImmortalWrt 25.12 (filogic / MT7981) cho Viettel NR3053 và 32X6.

            Mỗi thiết bị có **4 file firmware** + `sha256sums`:

            ### Viettel NR3053

            | File | Thành phần | Cách dùng |
            |------|------------|-----------|
            | `immortalwrt-mediatek-filogic-viettel_nr3053-squashfs-sysupgrade.itb` | Firmware | sysupgrade / TFTP (bootmenu [5]) |
            | `immortalwrt-mediatek-filogic-viettel_nr3053-initramfs-recovery.itb` | Recovery RAM | UART / TFTP test (bootmenu [2]) |
            | `immortalwrt-mediatek-filogic-viettel_nr3053-bl31-uboot.fip` | BL31 + U-Boot (FIP) | Nâng cấp U-Boot (bootmenu [7]) |
            | `immortalwrt-mediatek-filogic-viettel_nr3053-preloader.bin` | BL2 preloader | Nâng cấp BL2 (bootmenu [8], **rủi ro cao**) |

            ### Viettel 32X6

            | File | Thành phần | Cách dùng |
            |------|------------|-----------|
            | `immortalwrt-mediatek-filogic-viettel_32x6-squashfs-sysupgrade.itb` | Firmware | sysupgrade / TFTP (bootmenu [5]) |
            | `immortalwrt-mediatek-filogic-viettel_32x6-initramfs-recovery.itb` | Recovery RAM | UART / TFTP test (bootmenu [2]) |
            | `immortalwrt-mediatek-filogic-viettel_32x6-bl31-uboot.fip` | BL31 + U-Boot (FIP) | Nâng cấp U-Boot (bootmenu [7]) |
            | `immortalwrt-mediatek-filogic-viettel_32x6-preloader.bin` | BL2 preloader | Nâng cấp BL2 (bootmenu [8], **rủi ro cao**) |

            Chi tiết nạp firmware: [docs/huong-dan-nap-firmware.md](https://github.com/quytttb/immortalwrt-mt798x-rebase/blob/main/docs/huong-dan-nap-firmware.md)

            Built from tag `${{ needs.prepare.outputs.tag }}`.
          files: |
            release-assets/**/*.itb
            release-assets/**/*.bin
            release-assets/**/*.fip
            release-assets/**/sha256sums
